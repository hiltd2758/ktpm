$kind: http-request
name: GET /api/patient/appointment/doctors
url: "{{base_url}}/api/patient/appointment/doctors"
method: GET
headers:
  Authorization: Bearer {{admin_token}}
auth:
  type: bearer
  credentials:
    token: "{{jwt_patient_token}}"
order: 6000
