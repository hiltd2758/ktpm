$kind: http-request
description: |-
  PUT /api/admin/doctor/update/password/{id} — Changes a doctor's password (BCrypt-encoded).

  Path variable: id (doctor ID)
  Body: { newPassword: string }
  Source: AdminApiController.updateDoctorPassword()
url: "{{base_url}}/api/admin/doctor/update/password/{{doctor_id}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "newPassword": "Doctor@123"
    }
auth:
  type: bearer
  credentials:
    token: "{{admin_token}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200 OK or 400 if doctor not found', function () {
          pm.expect([200, 400]).to.include(pm.response.code);
      });

      if (pm.response.code === 200) {
          pm.test('Success message present', function () {
              var body = pm.response.json();
              pm.expect(body).to.have.property('message');
          });
      }
    language: text/javascript
order: 2000
